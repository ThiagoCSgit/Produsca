import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    iconCart: {
      position: 'relative',
      left: 30
    },
    modifyPositionIcon: {
      position: 'relative',
      left: 60
    },
  });


export default styles;